function solution(num1, num2) {
    // (num1 / num2) * 1000
    // 소수점 날려버리기 Math.floor()
    let answer = 0;
    
    // answer = Math.floor((num1 / num2) * 1000);
    
    return answer = Math.floor((num1 / num2) * 1000);
}